# Enterprise Application Integration

## Contents:

1. Example of simple RESTFul API using Laravel & MySQL (Monolith).
2. Example of simple RESTFul API using Laravel & MySQL (Microservices).
    - Example of direct communication between services (service-to-service) and synchronously.
    - Example of implementing microservices with Docker (multi-repo) and RabbitMQ for asynchronous communication.
3. Example of simple GraphQL API Service using Laravel & MySQL.